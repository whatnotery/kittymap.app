<script>

let catData = {
<% @cats.each do |cat|%>
<%= "#{cat.id}"%> :
  <%= cat.to_json.html_safe %>,
<% end %>
}  

</script>


<div class="h-screen">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>
 
    <div id="cats" class="float-left w-1/4 h-full	flex overflow-auto">
      <%= render @cats %>
    </div>
  
    <div id='map' class="bw-5/10 h-screen">t</div>

</div>
