<script>

let catData = {
<% @cats.each do |cat|%>
<%= "#{cat.id}"%> :
  <%= cat.to_json.html_safe %>,
<% end %>
}  

</script>


<div class="grid gap-2 ">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="grid col-span-2">  
    <div id="cats" class="">
      <%= render @cats %>
    </div>
  </div>
  <div class="grid col-span-4">
    <div id='map'></div>
  </div>









</div>
